// when sending any command, check if group already exists on splitwise
// if not, create group and add participants before executing command

// command: adicionar despesa
// command: rateio despesa
// command: editar valor despesa

// hook: when adding user to chat, add to group on slplitwise (?)
