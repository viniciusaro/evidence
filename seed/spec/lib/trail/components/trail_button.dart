import 'package:spec/trail.dart';

class TrailButton extends StatelessWidget {
  const TrailButton({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
