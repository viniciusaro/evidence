import 'package:spec/trail.dart';

class TrailTextField extends StatelessWidget {
  const TrailTextField({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
